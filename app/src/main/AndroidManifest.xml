<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lucas.magnus.academia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".AddAlunoActivity" />
        <activity android:name=".AddModalidadeActivity" />
        <activity android:name="com.lucas.magnus.academia.ModalidadesActivity" />
        <activity android:name=".AlunosActivity" />
        <activity android:name=".AddPlanoActivity" />
        <activity android:name=".PlanosActivity" />
        <activity android:name=".SelecionaAlunoActivity" />
        <activity android:name=".AddMatriculaActivity" />
        <activity android:name=".MatriculasActivity" />
        <activity android:name=".SelecionaCidadeActivity" />
        <activity android:name=".GerarFaturaoActivity" />
        <activity android:name=".FaturasActivity" />
    </application>

</manifest>